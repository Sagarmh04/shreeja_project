# Supabase Setup

Run these SQL files in order:

1. `001_schema.sql`
2. `002_rls.sql`
3. `003_seed.sql`

If your project was already initialized before audit target labels were added, also run:

4. `004_audit_target_label.sql`
